{
  "name": "qencode-monitoring",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "deploy": "gcloud --project=$GCP_PROJECT functions deploy beginNewEncodingHealthCheck --runtime=nodejs10 --trigger-http --source=. --max-instances=1 --memory=128MB --timeout=500s",
    "pretest": "eslint src",
    "test": "mocha test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/tasks": "^2.0.0",
    "gaxios": "^3.0.3",
    "tus-js-client": "^2.1.1"
  },
  "devDependencies": {
    "eslint": "^7.0.0",
    "mocha": "^7.1.2"
  },
  "eslintConfig": {
    "env": {
      "node": true
    },
    "parserOptions": {
      "ecmaVersion": 2019
    },
    "extends": "eslint:recommended",
    "rules": {
      "prefer-arrow-callback": "error",
      "indent": [
        "error",
        2,
        {"MemberExpression": 0}
      ]
    }
  }
}
